services:
  listener:
    build: .
    env_file: .env
    restart: unless-stopped
    volumes:
      - listener_data:/app/data
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
volumes:
  listener_data:
